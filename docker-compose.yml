version: '3.8'

services:
  # api:
  #   build: .
  #   ports:
  #     - "5000:5000"
  #   environment:
  #     - REDIS_URL=redis://redis:6379/0
  #   depends_on:
  #     - redis
  #   env_file:
  #     - .env

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data: